# SC Signature Scanner
# Star Citizen signature detection and identification tool
# Author: Mallachi

# ===== OCR Engine =====
# Primary: Windows OCR (Windows.Media.Ocr) — built into Windows 10/11.
# Faster, no model download, better accuracy on clean UI text. Reached
# from Python via the modular winrt-* prebuilt-wheel packages.
winrt-runtime>=3.0.0
winrt-Windows.Media.Ocr>=3.0.0
winrt-Windows.Globalization>=3.0.0
winrt-Windows.Graphics.Imaging>=3.0.0
winrt-Windows.Storage.Streams>=3.0.0
winrt-Windows.Foundation>=3.0.0
winrt-Windows.Foundation.Collections>=3.0.0

# Fallback: EasyOCR (deep learning OCR). Used only when Windows OCR is
# unavailable (English language profile not installed, etc.). First run
# downloads ~115MB of model files to ~/.EasyOCR/model/.
easyocr>=1.7.0

# PyTorch - Required by EasyOCR (CPU version)
# Note: On Windows, torch must be installed before easyocr
# Install with: pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
torchvision>=0.15.0

# ===== Image Processing =====
Pillow>=10.0.0
numpy>=1.24.0
opencv-python>=4.8.0

# ===== File Monitoring =====
watchdog>=3.0.0

# ===== UI =====
# pywebview - desktop window hosting the React UI in ui/main/
# Uses Microsoft Edge WebView2 on Windows (preinstalled on Win10/11).
pywebview>=6.2.0

